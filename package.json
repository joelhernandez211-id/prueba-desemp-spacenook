{
  "name": "space-booking",
  "version": "1.0.0",
  "description": "SPA for workspace reservation management",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
